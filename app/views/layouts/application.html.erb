<!DOCTYPE html>
<html>
	<head>
  	<title>N&oslash;skvabbel</title>
	  <%= stylesheet_link_tag :all %>
	  <%= javascript_include_tag :defaults %>
	  <%= csrf_meta_tag %>
	</head>
	<body>
		
		<% if flash[:notice] %>
			<p id="flash" title="click to close" onclick="this.shrink();"><%= flash[:notice] %></p>
		<% end %>
		
		<div id="container">

			<div id="header">

				<h1 id="application"><%= link_to 'N&oslash;skvabbel'.html_safe, '/', :class => 'no_color' %></h1>
				
				<% if current_user %>
					<h3>&nbsp;-&nbsp;Hi <span class="<%= gender(current_user) %>"> <%= current_user.username %></span>!
					<% if current_user.couple %>
						You're in couple with <span class="<%= gender(other_mate_in_couple_with(current_user)) %>"><%= other_mate_in_couple_with(current_user).username %></span>.
					<% else %>
      			Not in couple yet? Don't worry.
					<% end %>
					</h3>
					<div id="account">
						<%= link_to 'Account', account_path %> |
						<%= link_to 'Logout', logout_path %>
					</div>
				<% else %>
					<h3>&nbsp;-&nbsp;Sharing expenses in a couple should be easy</h3>
				<% end %>
			
			</div>
			
			<div id="content">
				<%= yield %>
			</div>

		</div>
	
	</body>
</html>
